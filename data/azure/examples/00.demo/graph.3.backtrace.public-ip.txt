Options summary:
  Not modeling source ip address spoofing (use -s flag to enable).
  Displaying endpoints only (use -r flag to display routing nodes). 
  Displaying paths (-p or -v).
  Verbose mode (-v).
Backprojecting paths past NAT rewrites. (-b)

Endpoints:
  Internet: 
  vm1/inbound: 
  vm1/outbound: 
  vm2/inbound: 
  vm2/outbound: 
  vm3/inbound: 
  vm3/outbound: 
  vm4/inbound: 
  vm4/outbound: 

Nodes reachable from publicIp1/inbound:

Internet:
  routes:
    (no routes)

  paths:
    (no paths)

vm1/inbound:
  routes:
    destination ip: 10.0.88.4
    destination port: ssh
    protocol: TCP

    source ip: vnet1, AzureLoadBalancer
    destination ip: 10.0.88.4

  paths:
    jump-box-ip => virtual-network => jump-box-subnet => jump-box948 => jump-box
      destination port: ssh
      protocol: TCP

      source ip: vnet1, AzureLoadBalancer

vm1/outbound:
  routes:
    (no routes)

  paths:
    (no paths)

vm2/inbound:
  routes:
    (no routes)

  paths:
    (no paths)

vm2/outbound:
  routes:
    (no routes)

  paths:
    (no paths)

vm3/inbound:
  routes:
    (no routes)

  paths:
    (no paths)

vm3/outbound:
  routes:
    (no routes)

  paths:
    (no paths)

vm4/inbound:
  routes:
    (no routes)

  paths:
    (no paths)

vm4/outbound:
  routes:
    (no routes)

  paths:
    (no paths)

