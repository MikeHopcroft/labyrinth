Options summary:
  Not modeling source ip address spoofing (use -s flag to enable).
  Displaying endpoints only (use -r flag to display routing nodes). 
  Displaying paths (-p or -v).
  Verbose mode (-v).
Backprojecting paths past NAT rewrites. (-b)

Endpoints:
  Internet: 
  vm1/inbound: 
  vm1/outbound: 
  vm2/inbound: 
  vm2/outbound: 
  vm3/inbound: 
  vm3/outbound: 
  vm4/inbound: 
  vm4/outbound: 

Nodes that can reach vm2/inbound:

Internet:
  routes:
    source ip: Internet
    destination ip: 52.183.88.218
    destination port: http, https
    protocol: TCP

  paths:
    Internet => public-services-ip => public-load-balancer => virtual-network => public-services-subnet => vm0148 => vm0

    Internet => public-services-ip => public-load-balancer => virtual-network => public-services-subnet => vm0148 => vm0


vm1/inbound:
  routes:
    (no routes)

  paths:
    (no paths)

vm1/outbound:
  routes:
    source ip: 10.0.88.4
    destination ip: 10.0.100.4
    destination port: ssh

  paths:
    jump-box => jump-box948 => jump-box-subnet => virtual-network => public-services-subnet => vm0148 => vm0
      source ip: 10.0.88.4
      destination ip: 10.0.100.4
      destination port: ssh

vm2/outbound:
  routes:
    (no routes)

  paths:
    (no paths)

vm3/inbound:
  routes:
    (no routes)

  paths:
    (no paths)

vm3/outbound:
  routes:
    (no routes)

  paths:
    (no paths)

vm4/inbound:
  routes:
    (no routes)

  paths:
    (no paths)

vm4/outbound:
  routes:
    (no routes)

  paths:
    (no paths)

